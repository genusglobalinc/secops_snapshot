# Python dependencies for secops_snapshot

# OpenAI client (AI assistance for report composition)
openai>=1.0.0

# Google APIs (Drive, Docs, Sheets)
google-api-python-client>=2.140.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# External CLI tools (install separately; package names shown for Ubuntu/Debian)
# - whois (package: whois)
# - dig (package: dnsutils)
# - curl (package: curl)
# - jq (package: jq)
# - pandoc (package: pandoc)  # for PDF generation
# - TeX engine for Pandoc PDF (texlive-latex-recommended texlive-fonts-recommended texlive-xetex)
# - whatweb (package: whatweb)
# - subfinder (ProjectDiscovery repo)

## Example install (WSL/Ubuntu):
# sudo apt-get update && sudo apt-get install -y \
#   whois dnsutils curl jq pandoc \
#   texlive-latex-recommended texlive-fonts-recommended texlive-xetex \
#   whatweb
# # For subfinder:
# sudo apt-get install -y golang && \
# GO111MODULE=on go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest && \
# echo 'export PATH="$HOME/go/bin:$PATH"' >> ~/.bashrc

